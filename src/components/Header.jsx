import React from "react";

function Header(){
    return(
        <header>
            <h1>Calculator</h1>
            <p>My first calculator using React.</p>
        </header>
    )
};

export default Header;